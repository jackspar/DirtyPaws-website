# Reconnaissance — Live Site + Namecheap Hosting (2026-06-03)
*Repo is PRIVATE as of 2026-06-03 (taken private while prototyping — Pages preview disabled as a consequence). Safe for internal notes.*
*Full session detail: `~/Projects/DirtyPaws/notes/2026-06-03-canva-connector-website-path.md`*

---

## A. Namecheap account recon (verified on-screen, logged-in session)

**Account:** DirtyPawsAR · account holder Kathleen Ellis · balance $0.00
**Access friction findings:** account-level 2FA = OFF (security gap — should be TOTP, ideally on John's phone as de facto webmaster); new-device logins trigger email verification routed to Kathleen (the dependency to eliminate); John's normal Brave holds a working logged-in session.

**Hosting Subscription (the jackpot):**

| Item | Value |
|---|---|
| Plan | **Stellar shared hosting — ACTIVE** (real cPanel hosting, not just the website builder) |
| Validity | **Mar 14, 2020 → Mar 14, 2027**, auto-renews yearly |
| Disk / bandwidth | 20 GB SSD / unmetered |
| Hosted domains | **3 allowed — only 1 used** (staging subdomain/site fits at zero cost) |
| Server | `server255.web-hosting.com` · IP `162.213.253.39` · US datacenter |
| cPanel | Installed · username `dirtndhb` · login via Namecheap "GO TO CPANEL" |
| cPanel apps visible | Email Accounts · Domains · **WordPress (installer)** · **Website Builder** · Namecheap SSL · ModSecurity |

**The architectural confirmation:** the live builder site's DNS A record (162.213.253.39) = this very cPanel server. The Website Builder and the WordPress installer are sibling apps on the same hosting. **WordPress can be installed alongside the live builder site — same plan, no new spend, no risk to production until DNS/docroot cutover.**

**Still to verify inside cPanel:** SSH/Terminal availability (needed for the chosen WP-CLI control path) · subdomain creation · PHP version options (server currently reports PHP 7.2.34 = EOL) · disk usage · mail accounts.

## B. Live-site recon summary (full version in the hub notes)

- Site last published **Feb 21, 2026**; byte-identical to the April 1 snapshot; **0 of the April audit's 17 issues fixed**
- **Critical:** adoption AND volunteer Google Form links use owner `/edit` URLs → every public visitor gets "Access Denied." Fix = `/viewform` suffix. Costing adoptions/volunteers weekly
- **Security:** Google Maps API key exposed in source on 4 pages (`AIzaSyD-9xtp38UunEmx7XfJ7eBh-K4w8qQ6SEw`) — restrict via Google Cloud Console referrer rules
- Stale: only event listed is "Feb. 21th, 2025" (15 months old, wrong ordinal) · About page says "Cats and Dogs" + typos ("oppurtunities," "adoped") · Twitter icon links to twitter.com/Twitter on all 9 pages
- SEO: generic titles ("Home"), meta description = page name, empty og:image, no favicon, logo alt="", www serves without redirect (duplicate content; SSL covers non-www only, expires Sep 29 2026), robots.txt 404
- Infra: LiteSpeed cPanel · PHP 7.2.34 (EOL) · MX = Namecheap private email (org actually uses personal Gmail dirtypawsanimalrescue@gmail.com) · indexing open · sitemap valid

## C. What this means — the plan, de-risked

1. **Quick wins on the LIVE site (builder edits, ~1 hour):** form URLs → `/viewform` · Events refresh · Twitter link · About fixes · titles/meta where the builder allows. Execution options: John in the builder, or Claude browser-extension session in his real Brave, with Claude Code verifying each fix live (curl) from the repo side.
2. **API key restriction (5 min, Google Cloud Console)** — independent of Namecheap.
3. **WordPress staging on THIS hosting:** install WP via cPanel installer to a subdomain (e.g. `new.dirtypawsanimalrescue.org`), migrate the existing 9-page prototype, Kathleen reviews, DNS/docroot cutover only on approval. Control path per John's decision: **WP-CLI over SSH** (verify SSH in cPanel first).
4. **Account hygiene:** enable TOTP 2FA (John's device), confirm account contact email, document cPanel credentials properly.

## D. Decisions on record

- WordPress control path = **WP-CLI over SSH** (Automattic WP-MCP plugin SKIPPED — John prefers CLI)
- Repo private while prototyping; flip public only if/when GitHub Pages preview is wanted again
- Recon method that works: John clicks in his real logged-in Brave; Claude reads via `screencapture` + image read. No debug-profile workbenches (they manufacture device-verification lockouts).
